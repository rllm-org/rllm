# @package _global_
#
# Everything verl reads natively -- `model`, `actor_rollout_ref`, `trainer` --
# for this recipe: FSDP2 actor/ref and the vLLM rollout engine. The `rllm.*`
# half is the sibling file `rllm_grpo.yaml`.
#
# Kept in YAML rather than as CLI flags in train_verl.sh so that a single
# decision lives in a single place: the length budget spans rllm.data.* and
# actor_rollout_ref.rollout.*, and when those sat in different files they
# drifted.
#
# Hardware assumption: 8 x 80GB, single node.

model:
  # Also consumed by the gateway to load the tokenizer for cumulative token mode.
  name: ${oc.env:MODEL_PATH,Qwen/Qwen3.5-4B}

actor_rollout_ref:
  hybrid_engine: true      # colocate vLLM and the FSDP actor, time-sliced

  model:
    path: ${model.name}
    trust_remote_code: true
    enable_gradient_checkpointing: true

    # Qwen3.5's vocab is 248320 tokens, so one 23K-token sequence's logits in
    # fp32 is a single ~21 GB allocation and OOMs an 80 GB card. The fused
    # linear+log-prob kernel computes log_probs/entropy in chunks off the hidden
    # states and never materializes logits. It consumes the packed
    # (remove-padding) layout, so these two go together.
    use_fused_kernels: true
    fused_kernel_options:
      impl_backend: torch
    use_remove_padding: true

  actor:
    # fsdp2 (per-parameter DTensor sharding) beat fsdp1 on every axis measured
    # here: 22.8 vs 27.1 GB peak, 215 vs 210 tok/s, update_actor 46.9 vs 62.8 s,
    # pearson 0.999745 vs 0.999662, and none of FSDP1's state_dict deprecation
    # warnings. verl also patches this model's vision tower specifically for the
    # fsdp2 cpu_offload path, so it is the path upstream expects.
    strategy: fsdp2

    # Sequence parallelism works only with the backported verl patch (see
    # scripts/apply_verl_patches.sh). Even patched it is a poor trade at 4B --
    # 30% less activation memory for 3.1x the actor update -- so leave it off
    # until activation memory is what binds.
    ulysses_sequence_parallel_size: 1

    # One sequence per micro-batch. >1 is numerically correct once the backport
    # is applied, but measured 2.01x slower per token (64.0 -> 128.6 us/tok) and
    # doubles activation memory, which at this context length OOMs: a single
    # 92K-token row needs ~30 GB, two packed rows did not fit on an 80 GB card.
    use_dynamic_bsz: false
    ppo_micro_batch_size_per_gpu: 1

    # rllm.data.train_batch_size (32) x rollout.n (8) = 64 trajectories; 8 x 8 = 64
    # rows here makes that exactly one optimizer update per step, i.e. strictly
    # on-policy GRPO.
    ppo_mini_batch_size: 8

    optim:
      lr: 1e-6
    entropy_coeff: 0.0
    fsdp_config:
      param_offload: true
      optimizer_offload: true

  ref:
    strategy: fsdp2
    ulysses_sequence_parallel_size: 1
    log_prob_micro_batch_size_per_gpu: 1
    fsdp_config:
      param_offload: true
    # NB: with rllm.algorithm.kl_beta=0 the reference worker is never created.

  rollout:
    name: vllm
    mode: async                      # required by the AgentFlow/gateway path
    tensor_model_parallel_size: 1    # -> one vLLM server per GPU
    gpu_memory_utilization: 0.50
    enforce_eager: false             # keep torch.compile + CUDA graphs
    free_cache_engine: true
    log_prob_micro_batch_size_per_gpu: 1
    val_kwargs:
      do_sample: true

    # = rllm.data.max_prompt_length + max_response_length. Overrunning this is
    # not a truncation: vLLM answers 400 mid-rollout, litellm's retries hit the
    # same wall, and the episode is discarded whole. One run at 32768 lost 28%
    # of its rollouts that way.
    max_model_len: 131072

    engine_kwargs:
      vllm:
        reasoning_parser: qwen3
        # mini-swe-agent v2 requires the model to answer with a 'bash' TOOL CALL,
        # and its litellm client always sends tool_choice="auto", which vLLM
        # rejects unless auto tool choice is on. qwen3_xml matches Qwen3.5's
        # <tool_call><function=..><parameter=..> template markup.
        enable_auto_tool_choice: true
        tool_call_parser: qwen3_xml

trainer:
  n_gpus_per_node: 8
  # rllm/trainer/verl/utils.py:240 forwards this as save_checkpoint's
  # max_ckpt_to_keep. An FSDP checkpoint here is bf16 weights plus fp32 Adam
  # state and master copy over 4.66B params -- on the order of 65 GB each -- so
  # an unbounded run fills a volume quickly.
  max_actor_ckpt_to_keep: 5
  nnodes: 1
  default_hdfs_dir: null
  resume_mode: disable
